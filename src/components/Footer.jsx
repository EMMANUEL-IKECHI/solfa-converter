import React from "react";

const Footer = () => {
  return (
    <footer className="bg-linear-to-r from-cyan-50 to-blue-50 border border-cyan-100 py-6 px-5 text-center text-gray-500 text-sm">
      Legend: do re mi fa so la ti do' = high do, so = low so
    </footer>
  );
};

export default Footer;
